name: Deploy Dungeon Veil Test Site

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  delegated-deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment ownership
        run: echo "Automatic Pages deployment runs inside the unified Dungeon RPG Check workflow."

# Trigger unified validation for Dungeon Veil branch commit 5f5d741.
