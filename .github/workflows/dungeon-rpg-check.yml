name: Dungeon RPG Check

on:
  push:
    branches: [main, work/blocks-1-7-production-pass]
    paths:
      - 'artifacts/dungeon-rpg/**'
      - 'lib/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/dungeon-rpg-check.yml'
      - '.github/workflows/dungeon-rpg-ci.yml'
  pull_request:
    branches: [main, work/blocks-1-7-production-pass]
    paths:
      - 'artifacts/dungeon-rpg/**'
      - 'lib/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/dungeon-rpg-check.yml'
      - '.github/workflows/dungeon-rpg-ci.yml'
      - '.ci/validation-trigger.txt'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  prepare-block-7:
    if: github.event_name == 'pull_request' && github.head_ref == 'fix/block-7-guild-invite-close'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/block-7-guild-invite-close
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Apply guild layout patch and self-clean
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main fix/block-1-email-confirmation-redirect
          git checkout origin/fix/block-1-email-confirmation-redirect -- \
            artifacts/dungeon-rpg/src/components/GuildPanelMobile.tsx \
            artifacts/dungeon-rpg/src/components/GuildSocialPanel.tsx \
            artifacts/dungeon-rpg/src/components/GuildInviteLinkCard.tsx \
            artifacts/dungeon-rpg/src/components/screens/MainMenuScreen.tsx \
            artifacts/dungeon-rpg/scripts/validate-social-navigation.mjs \
            artifacts/dungeon-rpg/scripts/validate-social-progression.mjs
          python3 .ci/block7_patch.py
          git checkout origin/main -- .github/workflows/dungeon-rpg-check.yml
          rm -f .github/workflows/build-block-7-guild.yml .ci/block7-trigger.txt .ci/block7_patch.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Move guild sharing into invite tab and add fixed close control'
          git push origin HEAD:fix/block-7-guild-invite-close

  typecheck-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Install workspace
        run: pnpm install --frozen-lockfile
      - name: Asset audit
        run: pnpm --filter @workspace/dungeon-rpg audit:assets
      - name: Room and collision audit
        run: pnpm --filter @workspace/dungeon-rpg audit:rooms
      - name: Typecheck Dungeon Veil
        run: pnpm --filter @workspace/dungeon-rpg typecheck
      - name: Build Dungeon Veil
        run: pnpm --filter @workspace/dungeon-rpg build
