name: Duo Matrix Diagnostics

on:
  pull_request:
    branches: ['fix/**']
    paths:
      - 'artifacts/dungeon-rpg/**'
      - '.github/workflows/duo-matrix-diagnostics.yml'

permissions:
  contents: read

jobs:
  duo-matrix-diagnostics:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Capture complete room audit chain
        shell: bash
        run: |
          set +e
          pnpm --filter @workspace/dungeon-rpg audit:rooms > duo-room-audit.log 2>&1
          echo $? > duo-room-audit.exit
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: duo-room-audit-diagnostics
          path: |
            duo-room-audit.log
            duo-room-audit.exit
          if-no-files-found: error
      - name: Enforce room audit chain
        shell: bash
        run: test "$(cat duo-room-audit.exit)" = "0"
