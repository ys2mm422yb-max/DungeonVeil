name: Dungeon RPG CI

# Validation trigger for wide ritual hall, mailbox and social systems 21840bb.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  delegated-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Validation ownership
        run: echo "Automatic Dungeon RPG CI runs are executed by the unified Dungeon RPG Check workflow."
