name: Public Testlink Main Menu Proof

on:
  pull_request:
    branches:
      - fix/mobile-telegraphs-room-21-50-balance
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-public-testlink:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Verify deployed main-menu bundle
        shell: bash
        run: curl --fail --silent --show-error --location https://raw.githubusercontent.com/ys2mm422yb-max/DungeonVeil/455d4aeb0cfe5f1170a76a3b84695804927f4ba8/.github/scripts/verify-public-testlink-main-menu.sh | bash
