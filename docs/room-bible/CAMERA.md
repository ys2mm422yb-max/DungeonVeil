# Mobile camera constraints

The run camera follows the player only inside a safe focus range. The player can still use the whole room, but the viewport no longer reveals black gaps or the un-authored outside staging area at room edges.